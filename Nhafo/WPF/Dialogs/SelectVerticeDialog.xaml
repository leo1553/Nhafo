<Dialog:CustomDialog x:Class="Nhafo.WPF.Dialogs.SelectVerticeDialog"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                     xmlns:local="clr-namespace:Nhafo.WPF.Dialogs"
                     mc:Ignorable="d" 
                     d:DesignWidth="500"
                     xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
                     Dialog:DialogParticipation.Register="{Binding}" >
    <Grid Margin="0,20,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Selecione um vértice:" />
        <ComboBox x:Name="comboBox" Height="30" Grid.Row="1"/>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="OK" Margin="10,0,0,0" Padding="25,12" Style="{StaticResource AccentedButton}" Click="OKButtonClick" Name="okButton" />
            <Button Content="Cancelar" Margin="10,0,0,0" Padding="25,12" Click="CancelButtonClick" />
        </StackPanel>
    </Grid>
</Dialog:CustomDialog>